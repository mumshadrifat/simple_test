class ApplicationReciever{
  int? id;
  String? engName;
  String? bnName;

  ApplicationReciever(this.id, this.engName, this.bnName);
}
class Service{
  int? id;
  String? engName;
  String? bnName;

  Service(this.id, this.engName, this.bnName);
}
class ServiceType{
  int? id;
  String? engName;
  String? bnName;

  ServiceType(this.id, this.engName, this.bnName);
}